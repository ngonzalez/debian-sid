---
apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: hebe
spec:
  type: LoadBalancer
  loadBalancerIP: APP_CLUSTER_IP
  selector:
    run: debian-sid
  ports:
    - port: 22
      name: ssh
      targetPort: 22
    - port: 80
      name: nginx
      targetPort: 8000
