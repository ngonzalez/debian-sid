---
apiVersion: v1
kind: Service
metadata:
  name: app-loadbalancer
  namespace: hebe
spec:
  type: LoadBalancer
  selector:
    run: debian-sid
  ports:
    - port: 80
      name: nginx
      targetPort: 8000
